tosca_definitions_version: alien_dsl_1_3_0

metadata:
  template_name: topology-capacility-prop-unkown
  template_version: 1.1.0-SNAPSHOT
  template_author: FastConnect

description: TOSCA simple profile with Apache.

imports:
  - "tosca-base-types:1.0"

topology_template:
  description: A simple topology template created directly from a CSAR archive
  node_templates:
    compute:
      type: tosca.nodes.Compute
      capabilities:
        compute:
          properties:
            # this capability property is not known
            stuff: something
