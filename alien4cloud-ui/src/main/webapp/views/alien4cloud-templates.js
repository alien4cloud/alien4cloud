// dev mode file replaced by a registration of all views in the angular template cache (see ngtemplate build task).
define(function () {
  'use strict';
  return function() {
    console.debug('Development mode, no templates to inject.');
  };
});
