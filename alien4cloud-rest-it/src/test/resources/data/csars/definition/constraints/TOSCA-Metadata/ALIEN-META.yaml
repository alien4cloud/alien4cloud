# Define the current archive id and version.
name: "constraint-sample"
version: "1.0"
license: "Apache v2.0"
created_by: "Alien 4 Cloud"

# List of definitions file in the archive.
definitions:
    - /Definitions/constraint-sample.yml