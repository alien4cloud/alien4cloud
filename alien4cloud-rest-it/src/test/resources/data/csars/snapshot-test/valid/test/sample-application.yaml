node_templates:
  compute_tomcat:
    type: tosca.nodes.Compute
    properties:
      os_type: linux
      os_distribution: centos
      os_version: centos

# scaling_policies:
dependencies:
  - name: tosca-base-types
    version: 1.0

# input_properties:

# input_artifacts:
