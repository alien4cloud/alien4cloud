node_templates:
  compute_tomcat:
    type: tosca.nodes.Compute
    properties:
      os_type: linux
      os_distribution: Ubuntu
      os_version: 14.0

# scaling_policies:
dependencies:
  - name: tosca-base-types
    version: 1.0

# input_properties:

# input_artifacts:
